{
	"name": "@action/docker-build-dispacth",
	"private": true,
	"type": "module",
	"main": "src/action.ts",
	"scripts": {
		"build": "bun build src/action.ts --target node --outfile index.js --minify",
		"clean": "rm index.js",
		"tsc:check": "tsc --noEmit",
		"tsc:check:watch": "tsc --noEmit --watch"
	},
	"dependencies": {
		"@actions/core": "^1.11.1",
		"@actions/github": "^6.0.1"
	},
	"devDependencies": {
		"@types/node": "^22.17.1",
		"@types/bun": "^1.3.2"
	}
}
